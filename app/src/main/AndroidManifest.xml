<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.family_artifact_register">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.MapManagerTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.Upload.PostActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <!--
        The API key for Google Maps-based APIs is defined as a string resource.
        (See the file "res/values/google_maps_api.xml").
        Note that the API key is linked to the encryption key used to sign the APK.
        You need a different API key for each encryption key, including the release key that is used to
        sign the APK for publishing.
        You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".FamilyArtifactRegisterActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".test.MapTestActivity"
            android:label="@string/title_activity_fullscreen" />
        <activity
            android:name=".UI.ArtifactHub.HubActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.ArtifactHub.ArtifactDetailActivity"
            android:parentActivityName=".UI.ArtifactHub.HubActivity" />
        <activity
            android:name=".UI.Social.FriendActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.Social.ContactDetailActivity"
            android:parentActivityName=".UI.Social.FriendActivity" />
        <activity
            android:name=".UI.Social.NewContactDetailActivity"
            android:parentActivityName=".UI.Social.ContactSearchActivity" />
        <activity
            android:name=".UI.Social.ContactSearchActivity"
            android:parentActivityName=".UI.Social.ContactDetailActivity" />
        <activity
            android:name=".UI.Social.ContactSearchResultActivity"
            android:parentActivityName=".UI.Social.ContactSearchActivity" />
        <activity
            android:name=".UI.ArtifactManager.ArtifactManageActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.ArtifactManager.NewArtifactActivity"
            android:parentActivityName=".UI.ArtifactManager.ArtifactManageActivity" />
        <activity android:name=".UI.ArtifactManager.NewArtifactActivity2" />
        <activity
            android:name=".UI.ArtifactManager.HappenedActivity"
            android:parentActivityName=".UI.ArtifactManager.ArtifactManageActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.mydomain.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver android:name=".Util.DownloadBroadcastHelper">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>
    </application> <!-- CAMERA -->
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>